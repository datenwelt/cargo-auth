[db.cargo_auth]
    type = "mysql"
    database = "cargo_auth"
    username = "cargo"
    password = "chieshoaC8Ingoob"
    port = 13701

    [db.cargo_auth.options]
        connectTimeout = 1000
        multipleStatements = true

[server]
    address = "0.0.0.0"
    port = "26571"
    access_log = "/tmp/access.log"
    error_log = "/tmp/error.log"

[server.routes.0]
    path = "/auth/login"
    module = "src/server/auth/login.js"

[rsa]
    privateKey = "test/data/rsa/privkey.encrypted.pem"
    passphrase = "test123456"

[logs]
    logfile = "/tmp/app.log"
    level = "DEBUG"

